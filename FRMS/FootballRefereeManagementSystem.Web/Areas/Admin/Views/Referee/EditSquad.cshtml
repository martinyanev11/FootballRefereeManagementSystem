@using FootballRefereeManagementSystem.Web.ViewModels.RefereeSquad;
@model RefereeSquadEditModel;
@{
    ViewData["Title"] = "Редакция на съдийска бригада";
}

<div class="container">
    <h1>@ViewData["Title"]</h1>
    <hr />
    <form method="post" asp-area="Admin" asp-controller="Referee" asp-action="EditSquad">
        <div class="row visually-hidden">
            <input asp-for="DivisionId" class="form-control" />
        </div>

        <div class="row">
            <div class="form-group mb-3 col-3">
                <label asp-for="MainRefereeName" class="dropdown-header">Главен съдия:</label>
                <input asp-for="MainRefereeName" class="form-control" disabled />
            </div>

            <div class="form-group mb-3 col-3">
                <label asp-for="MainRefereeId" class="dropdown-header">Промени главен съдия:</label>
                <span asp-validation-for="MainRefereeId" class="text-danger"></span>
                <select asp-for="MainRefereeId" class="dropdown-menu-start form-control">
                    @foreach (RefereeListModel referee in Model.MainRefereesList)
                    {
                        <option class="dropdown-item" value="@referee.Id">@referee.FullName</option>
                    }
                </select>
            </div>
        </div>

        <div class="row">
            <div class="form-group mb-3 col-3">
                <label asp-for="AssistantRefereeOneName" class="dropdown-header">Първи помощник съдия:</label>
                <input asp-for="AssistantRefereeOneName" class="form-control" disabled />
            </div>

            <div class="form-group mb-3 col-3">
                <label asp-for="AssistantRefereeOneId" class="dropdown-header">Промени първи помощник съдия:</label>
                <span asp-validation-for="AssistantRefereeOneId" class="text-danger"></span>
                <select asp-for="AssistantRefereeOneId" class="dropdown-menu-start form-control">
                    @foreach (RefereeListModel referee in Model.AssistantRefereesList)
                    {
                        <option class="dropdown-item" value="@referee.Id">@referee.FullName</option>
                    }
                </select>
            </div>
        </div>

        <div class="row">
            <div class="form-group mb-3 col-3">
                <label asp-for="AssistantRefereeTwoName" class="dropdown-header">Втори помощник съдия:</label>
                <input asp-for="AssistantRefereeTwoName" class="form-control" disabled />
            </div>

            <div class="form-group mb-3 col-3">
                <label asp-for="AssistantRefereeTwoId" class="dropdown-header">Промени втори помощник съдия:</label>
                <span asp-validation-for="AssistantRefereeTwoId" class="text-danger"></span>
                <select asp-for="AssistantRefereeTwoId" class="dropdown-menu-start form-control">
                    @foreach (RefereeListModel referee in Model.AssistantRefereesList)
                    {
                        <option class="dropdown-item" value="@referee.Id">@referee.FullName</option>
                    }
                </select>
            </div>
        </div>

        <div class="row">
            <div class="form-group mb-3 col-3">
                <label asp-for="DelegateName" class="dropdown-header">Делегат:</label>
                <input asp-for="DelegateName" class="form-control" disabled />
            </div>

            <div class="form-group mb-3 col-3">
                <label asp-for="DelegateId" class="dropdown-header">Промени делегат:</label>
                <span asp-validation-for="DelegateId" class="text-danger"></span>
                <select asp-for="DelegateId" class="dropdown-menu-start form-control">
                    @foreach (RefereeListModel referee in Model.DelegatesList)
                    {
                        <option class="dropdown-item" value="@referee.Id">@referee.FullName</option>
                    }
                </select>
            </div>
        </div>

        <button type="submit" class="btn btn-warning">Редактирай бригада</button>
        <a class="btn btn-secondary" asp-area="" asp-controller="Referee" asp-action="Schedule">Отказ</a>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}